# Paper trading works without Kalshi credentials.
SFO_FORECASTER_ROOT=/absolute/path/to/WeatherEdge/forecaster
SFO_KALSHI_DB=/absolute/path/to/WeatherEdge/trading/data/paper_trading.db
# No daily spend cap: exposure is risk-gated per position, per target date,
# and one entry per market/side.
PAPER_BANKROLL=1000
PAPER_RISK_PROFILE=balanced

# Optional later, only needed for authenticated/demo/live endpoints.
# Keep live trading disabled until paper trading has passed.
KALSHI_API_KEY_ID=
KALSHI_PRIVATE_KEY_PATH=
KALSHI_ENV=prod
